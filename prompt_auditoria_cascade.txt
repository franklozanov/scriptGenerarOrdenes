Cascade, estamos experimentando un "silent failure" (fallo silencioso): el menú principal "Gestionar OA" no se visualiza en la UI de Google Sheets. Sabemos que Google Sheets oculta los menús que no tienen ítems, lo que significa que `hasPermission` está retornando `false` para todas las opciones del `mainMenu` en `onOpen()`.

Por favor, realiza una REVISIÓN PROFUNDA Y DIAGNÓSTICO del flujo de permisos y aplica las correcciones arquitectónicas necesarias.

Archivos a auditar y corregir: Main.gs y Permissions.gs

Tareas de Revisión y Corrección:

1. 🔍 Problema de Desconexión de Roles (Usuarios vs PermisosRoles): Los roles insertados por defecto son "ADMIN", "QA" y "STANDARD". Si en la hoja 'Usuarios' el usuario actual se llama "Administrador", `getUserPermissions` devolverá `{}`. 
   - En `Main.gs`, actualiza el fallback de seguridad: `var isAdminFallback = (validUser.rol === 'Administrador' || validUser.rol === 'ADMIN' || validUser.rol === 'Administrador de Sistema');` para cubrir variaciones.

2. 🐛 Problema de Doble Lectura en `hasPermission`: 
   - En `Permissions.gs`, `hasPermission(userId, permiso)` está llamando a `getUserRecordByUserId_(userId)`. En el `onOpen`, hacemos esto repetidas veces por cada botón, leyendo la hoja 'Usuarios' muchas veces, lo que satura la cuota y ralentiza la carga.
   - Crea una nueva función: `function hasPermissionByRol(rol, permisoRequerido)` que simplemente llame a `getUserPermissions(rol)` y verifique el permiso SIN consultar la hoja de usuarios nuevamente.

3. 🛠️ Refactorización de `onOpen` en `Main.gs`:
   - Modifica las validaciones del menú en `Main.gs` para usar la nueva función. Ejemplo: `if (hasPermissionByRol(validUser.rol, PERMISOS.CARGAR_ORDENES))`.
   - Añade un bloque de log de diagnóstico estricto antes de armar el menú: `Logger.log("Diagnóstico Menú - Rol: " + validUser.rol + " | Permisos obtenidos: " + JSON.stringify(getUserPermissions(validUser.rol)));`
   - Si `getUserPermissions(validUser.rol)` devuelve un objeto vacío, lanza un `SpreadsheetApp.getUi().toast('Atención: Tu rol "' + validUser.rol + '" no tiene permisos configurados.', 'Sistema de Permisos', 10);` para alertar visualmente al usuario.

REGLA ESTRICTA FINAL: Aplica estas modificaciones en `Main.gs` y `Permissions.gs`. Al terminar, detente y genera tu micro-resumen con los cambios aplicados. Adicionalmente, proporciona instrucciones claras de qué revisar MANUALMENTE en las hojas "Usuarios" y "PermisosRoles" para asegurar que los nombres de los roles coincidan y solventar el problema definitivamente.