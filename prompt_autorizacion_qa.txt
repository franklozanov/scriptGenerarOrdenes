Cascade, los permisos RBAC han sido configurados exitosamente en la hoja de cálculo, auditados, y el menú principal ya se visualiza correctamente. Ahora procedamos a implementar el flujo crítico de "Autorización de Órdenes QA" para cumplir con la 21 CFR Part 11, aprovechando nuestro nuevo motor de permisos.

Por favor, implementa este flujo siguiendo este protocolo estricto paso a paso. Recuerda la REGLA ESTRICTA: ejecuta SOLO UN PASO, genera el micro-resumen compacto y pide autorización para continuar.

---

PASO 1: Obtención de Órdenes Pendientes de Aprobación
Archivo a modificar: NovedadLogic.gs
- Crea una nueva función llamada `getOrdenesSolicitadasParaQA()`.
- Esta función debe recorrer la hoja 'Ordenes' y retornar un array de objetos exclusivamente con aquellas órdenes que tengan `STATUS === 'Solicitada'`.
- El objeto a retornar por cada fila debe incluir: `noOrden`, `codigo`, `descripcion`, `lote` y `estadoCarga`.
- Envuelve el bloque en un try-catch y añade logs adecuados.

---

PASO 2: Integración del Backend de Autorización y UI
Archivos a modificar: Main.gs y NovedadLogic.gs
- En `Main.gs`, dentro de la función `onOpen()`, bajo la sección de construcción del menú `mainMenu`, añade un nuevo ítem condicionalmente usando la función optimizada: `if (hasPermissionByRol(validUser.rol, PERMISOS.AUTORIZAR_QA)) { mainMenu.addItem('✅ Autorizar Órdenes (QA)', 'abrirModalAutorizarQA'); }`.
- En `NovedadLogic.gs`, crea la función `abrirModalAutorizarQA()` que levante (usando `showModelessDialog`) un nuevo archivo llamado `ModalAutorizarQA.html` con dimensiones de 950x700 y título "Autorizar Órdenes (QA)".

---

PASO 3: Creación de la Interfaz Frontend (Modal)
Archivo a crear: ModalAutorizarQA.html (NUEVO ARCHIVO)
- Crea este archivo incluyendo la estructura base, llamadas a `Theme.html` y `GlobalScripts.html`.
- Inicializa `initializeModalDragResize` con título "Autorización de Órdenes (QA)" y usa el `onReady` o `window.onload` para llamar a `getOrdenesSolicitadasParaQA()` y popular la tabla.
- Añade una tabla HTML dinámica. Columnas: Checkbox de selección, No. Orden, Código, Lote y Estado Documentos.
- Agrega un bloque debajo de la tabla para ingresar el "PIN de Autorización (Firma Electrónica) *" obligatorio.
- Agrega un botón "✅ Firmar y Autorizar Seleccionadas" que valide el PIN, llame al Web App (`operation: 'autorizarOrdenesQA'`) pasando los `targetIds` seleccionados y el PIN, y muestre un overlay de carga.

---

PASO 4: Lógica de Procesamiento y Auditoría (Endpoint)
Archivos a modificar: WebApp.gs y NovedadLogic.gs
- En `WebApp.gs`, dentro de `handlePrivilegedOperation_`, añade el bloque para `operation === 'autorizarOrdenesQA'`. Protégelo con `hasPermission(callingUserId, PERMISOS.AUTORIZAR_QA)`. Dirígelo a `procesarAutorizacionQA(params, callingUserId)`.
- En `NovedadLogic.gs`, crea `procesarAutorizacionQA(params, userId)`:
  - Recorre la hoja 'Ordenes'. Si el `NoOrden` está en `params.targetIds` y el STATUS es 'Solicitada', actualízalo a 'Autorizada'.
  - Importante: si `estadoCarga` de la orden a aprobar es `Pendiente COA/OA`, bloquea y retorna error para esa orden.
  - Actualiza la columna `SolicitadaPor` agregando la nueva firma QA (ej: valor original + " | Autorizado QA: " + userId + " - " + timestamp).
  - Registra el evento en 'Logs': `logChange('AUTORIZACION_QA', 'Órdenes autorizadas: ' + params.targetIds.join(', '), userIdentity)`.

REGLA ESTRICTA FINAL: Empieza SÓLO con el PASO 1, dame el micro-resumen y pide autorización.