Cascade, antes de continuar con el módulo de notificaciones, necesitamos realizar un DIAGNÓSTICO COMPLETO del flujo de impresión actual. El objetivo es confirmar si el sistema ya restringe de forma segura la impresión *únicamente* a órdenes con estado 'Autorizada' o que tengan una solicitud extraordinaria (Reimpresión/Adicional) 'Aprobada' por QA.

Por favor, analiza rigurosamente los siguientes archivos: Index.html y PrintLogic.gs.

Tareas de Diagnóstico:
1. Analiza `fetchOrderData` en `PrintLogic.gs`: ¿Qué sucede exactamente cuando un operario ingresa un número de orden? Detalla de forma técnica qué estados son bloqueados, cuáles son permitidos y cómo se maneja la validación de la hoja 'SolicitudesImpresion'.
2. Analiza el comportamiento en `Index.html`: ¿Cómo reacciona la interfaz (`preloadOrderData`) al estado de la orden devuelto por el servidor? ¿Se bloquea correctamente el botón `#goBtn`? ¿Aparece el panel de solicitud de QA cuando la orden ya está impresa?
3. Analiza la Experiencia de Usuario (UX) e Interfaz: Actualmente, el campo de orden es un `<input type="text">` libre. El operario debe adivinar o saber el número de orden a imprimir. Evalúa si el blindaje reactivo del backend es suficiente para la normativa 21 CFR Part 11, o si sería recomendable refactorizar el `getInitialData` y el `Index.html` para incluir un `<datalist>` (o `<select>`) que precargue y muestre *únicamente* los números de las órdenes que cumplen con los requisitos (Autorizadas o con Solicitud Aprobada), evitando que el operario escriba órdenes inválidas.

REGLA ESTRICTA FINAL: Al terminar tu revisión, detente por completo. Genera un reporte de diagnóstico compacto con tus hallazgos. Luego:
- Si el sistema ya está 100% cubierto y consideras que el input libre es suficiente, indícalo y pregunta si procedemos con las Notificaciones.
- Si recomiendas cambiar el input libre a una lista desplegable/datalist de órdenes válidas, propón el plan de acción (PASO 1) para implementarlo y pide autorización.